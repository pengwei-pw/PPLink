server:
  port: 8081

spring:
  #数据源连接配置
  datasource:
    url: jdbc:mysql://47.103.42.78:3306/mydb?useSSL=false&characterEncoding=utf8
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: jj$$pengwei@test.jj#@$

#整合myBatis
mybatis:
  mapper-locations: classpath:mapper/*.xml   # mapper映射对应的配置文件位置.xml
  type-aliases-package: com.pw.pplink.entity     # 对应的实体类的包名
  configuration:
    map-underscore-to-camel-case: true       #字段名下划线和实体类驼峰命名的属性名对应